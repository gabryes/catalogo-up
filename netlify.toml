[build]
  command = "pip install -r requirements.txt"
  functions = "netlify/functions"
  publish = "public"

[env.production]
  environment = { GOOGLE_SHEETS_KEY = "", GOOGLE_CREDENTIALS_PATH = "credentials.json" }